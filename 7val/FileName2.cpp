#include <iostream>
#include <stack> // In case if you want to use stack
#include <string>

string decodeAndModifyString(const string& s) {
    stack <string> ss;
    ss.push("a");
    // s = ss.pop();
    return s;
    // Your solution here
}